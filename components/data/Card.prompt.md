The base surface container. Soft `surface-1` fill, 1px border, 12px radius. Pass `title`/`headerRight` for a panel header; use `padding="none"` to embed tables edge-to-edge.

```jsx
<Card title="Segment breakdown" padding="none"><table>…</table></Card>
<Card>Plain body content</Card>
```
