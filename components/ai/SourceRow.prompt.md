A "sources in scope" list row. The numbered blue square matches the `RefBadge` citing it.

```jsx
<SourceRow n={1} name="Revenue warehouse — Q3 rollup" onAction={preview} />
<SourceRow n={2} name="Renewals tracker (CSV)" onAction={preview} />
```
