The product top bar. Left: the Blue Insights square mark ("b") + product name + breadcrumb. Right: utility controls then the user avatar.

```jsx
<AppBar product="Blue Insights" breadcrumb="FY26 Revenue" initials="AM"
  right={<><IconButton label="Reset demo">…</IconButton><IconButton label="Dark">…</IconButton></>} />
```

The "b" mark is a typographic treatment (first letter of the product name in a Neural-Blue rounded square), not a supplied logo file.
